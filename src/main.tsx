import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './burger/App';
import './index.css';
import './burger/Burger.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App/>
  </React.StrictMode>,
)
